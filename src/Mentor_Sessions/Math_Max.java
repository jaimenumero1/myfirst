package Mentor_Sessions;

public class Math_Max {

   /* int num1, num2, num3, numMax;
    Scanner input = new Scanner(System.in);
        System.out.print("Input 3 numbers: ");
    num1 = input.nextInt();
    num2 = input.nextInt();
    num3 = input.nextInt();
    numMax = Math.max(num1, num2);
    numMax = Math.max(numMax, num3); //cool
//        numMax = Math.max(Math.max(num1, num2), num3);
        System.out.println("Max number of three is " + numMax);*/
}
